[Unit]
Description=Setup Hub network
Requires=hub-deps.service docker.service
After=hub-deps.service docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/hub run-script network
RemainAfterExit=true

[Install]
WantedBy=multi-user.target
